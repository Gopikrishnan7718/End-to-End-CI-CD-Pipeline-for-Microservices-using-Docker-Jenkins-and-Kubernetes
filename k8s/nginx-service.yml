apiVersion: v1
kind: Service
metadata:
   name: nginx

spec:
  type: NodePort
  ports:
    - targetPort: 80
      port: 80
      nodePort: 30001

  selector:
      app: reverse-proxy
      type: nginx

